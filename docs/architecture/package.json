{
  "name": "architecture",
  "version": "1.0.0",
  "description": "This directory holds the Mealie architecture documentation as code, using the [C4 model](https://c4model.com/). The system is described in a single `.c4` file (Structurizr DSL) alongside any rendered exports (SVG/PNG) committed for review convenience. Edit the `.c4` source as the source of truth; regenerate exports rather than hand-editing them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "likec4": "^1.56.0"
  }
}
